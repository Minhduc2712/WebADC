@using ErpOnlineOrder.Application.DTOs
@using ErpOnlineOrder.Application.DTOs.OrderDTOs
@model PagedResult<OrderDTO>
@{
    ViewData["Title"] = "Đơn hàng của tôi";
    Layout = "~/Views/Shared/_ShopLayout.cshtml";
}

<section class="blog about-blog">
    <div class="container">
        <div class="blog-bradcrum">
            <span><a asp-action="Account">Tài khoản</a></span>
            <span class="devider">/</span>
            <span><a href="#">Đơn hàng của tôi</a></span>
        </div>
        <div class="blog-heading about-heading">
            <h1 class="heading">Danh sách đơn hàng</h1>
        </div>
    </div>
</section>

<section class="product footer-padding">
    <div class="container">
        @await Html.PartialAsync("_AccountNavbar")
        
        <div class="row justify-content-center mt-4">
            <div class="col-lg-12">
                <div class="card shadow-sm border-0">
                    <div class="card-body p-4">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h5 class="fw-bold mb-0">Quản lý đơn hàng</h5>
                            <form method="get" class="d-flex gap-2">
                                <select name="status" class="form-select form-select-sm" onchange="this.form.submit()">
                                    <option value="">Tất cả trạng thái</option>
                                    <option value="Pending" selected="@(ViewBag.Status == "Pending")">Chờ xử lý</option>
                                    <option value="Approved" selected="@(ViewBag.Status == "Approved")">Đã duyệt</option>
                                    <option value="Completed" selected="@(ViewBag.Status == "Completed")">Hoàn thành</option>
                                    <option value="Cancelled" selected="@(ViewBag.Status == "Cancelled")">Đã hủy</option>
                                    <option value="Split" selected="@(ViewBag.Status == "Split")">Đã tách</option>
                                </select>
                            </form>
                        </div>

                        <div class="table-responsive">
                            <table class="table table-hover align-middle">
                                <thead class="table-light">
                                    <tr>
                                        <th>Mã đơn</th>
                                        <th>Ngày đặt</th>
                                        <th class="text-end">Tổng tiền</th>
                                        <th class="text-center">Trạng thái</th>
                                        <th class="text-end">Thao tác</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model.Items != null && Model.Items.Any())
                                    {
                                        foreach (var item in Model.Items)
                                        {
                                            var isChild = item.IndentLevel > 0;
                                            var rowClass = isChild ? "bg-light" : ""; 
                                            var paddingLeft = isChild ? "ps-5" : "ps-3";

                                            <tr class="@rowClass">
                                                <td class="@paddingLeft">
                                                    @if (isChild)
                                                    {
                                                        <span class="text-muted me-1">|_</span>
                                                        <a asp-action="OrderDetail" asp-route-id="@item.Id" class="text-decoration-none text-secondary fw-bold" title="Đơn con tách từ đơn gốc">
                                                            #@item.Order_code
                                                        </a>
                                                        <span class="badge bg-info ms-2" style="font-size: 0.7em;">Đơn tách</span>
                                                    }
                                                    else
                                                    {
                                                        <a asp-action="OrderDetail" asp-route-id="@item.Id" class="text-decoration-none fw-bold text-dark">
                                                            #@item.Order_code
                                                        </a>
                                                    }
                                                </td>
                                                <td>@item.Order_date.ToString("dd/MM/yyyy HH:mm")</td>
                                                <td class="text-end"><strong class="text-danger">@item.Total_price.ToString("N0") đ</strong></td>
                                                <td class="text-center">
                                                    @if(item.Order_status == "Pending") { <span class="badge bg-warning text-dark">Chờ xử lý</span> }
                                                    else if(item.Order_status == "Exporting") { <span class="badge bg-primary">Xuất kho</span> }
                                                    else if(item.Order_status == "Approved") { <span class="badge bg-primary">Đã duyệt</span> }
                                                    else if(item.Order_status == "Completed") { <span class="badge bg-success">Hoàn thành</span> }
                                                    else if(item.Order_status == "Cancelled") { <span class="badge bg-danger">Đã hủy</span> }
                                                    else if(item.Order_status == "WaitingCustomer") { <span class="badge bg-warning text-dark">Chờ xác nhận</span> }
                                                    else if(item.Order_status == "Split") { <span class="badge bg-secondary">Đã tách</span> }
                                                    else { <span class="badge bg-secondary">@item.Order_status</span> }
                                                </td>
                                                <td class="text-end">
                                                    <a asp-action="OrderDetail" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary rounded-pill px-3">
                                                        <i class="fas fa-eye"></i> Theo dõi
                                                    </a>
                                                    @if (item.Order_status == "WaitingCustomer")
                                                    {
                                                        <button type="button" class="btn btn-sm btn-success rounded-pill px-3 ms-1" onclick="approveOrder(@item.Id)">
                                                            <i class="fas fa-check"></i> Duyệt
                                                        </button>
                                                        <button type="button" class="btn btn-sm btn-outline-danger rounded-pill px-3 ms-1" onclick="rejectOrder(@item.Id)">
                                                            <i class="fas fa-times"></i> Không duyệt
                                                        </button>
                                                    }
                                                </td>
                                            </tr>
                                        }
                                    }
                                    else
                                    {
                                        <tr>
                                            <td colspan="5" class="text-center py-5 text-muted">
                                                <i class="fas fa-box-open fa-3x mb-3 text-light"></i><br/>
                                                Bạn chưa có đơn hàng nào. <a asp-action="Products" class="text-primary text-decoration-none">Tiếp tục mua sắm</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                        
                        <!-- Pagination -->
                        @if (Model.TotalPages > 1)
                        {
                            <nav class="mt-4">
                                <ul class="pagination justify-content-center">
                                    <li class="page-item @(Model.Page == 1 ? "disabled" : "")">
                                        <a class="page-link" asp-action="Orders" asp-route-page="@(Model.Page - 1)" asp-route-status="@ViewBag.Status">Trước</a>
                                    </li>
                                    @for (int i = 1; i <= Model.TotalPages; i++)
                                    {
                                        <li class="page-item @(Model.Page == i ? "active" : "")">
                                            <a class="page-link" asp-action="Orders" asp-route-page="@i" asp-route-status="@ViewBag.Status">@i</a>
                                        </li>
                                    }
                                    <li class="page-item @(Model.Page == Model.TotalPages ? "disabled" : "")">
                                        <a class="page-link" asp-action="Orders" asp-route-page="@(Model.Page + 1)" asp-route-status="@ViewBag.Status">Sau</a>
                                    </li>
                                </ul>
                            </nav>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
<script>
    async function handlePost(url, payload = null) {
        try {
            const options = {
                method: 'POST',
                headers: {
                    'X-Requested-With': 'XMLHttpRequest' // Báo cho server biết đây là AJAX
                }
            };
            
            // Chỉ gửi Content-Type JSON khi thực sự có dữ liệu truyền đi
            if (payload) {
                options.headers['Content-Type'] = 'application/json';
                options.body = JSON.stringify(payload);
            }
            
            const res = await fetch(url, options);
            
            // Xử lý mượt mà nếu bị mất Session đăng nhập
            if (res.status === 401 || res.status === 403 || res.redirected) {
                window.location.href = '/Auth/Login';
                return { success: false, message: "Phiên đăng nhập đã hết hạn. Vui lòng đăng nhập lại." };
            }
            
            const contentType = res.headers.get("content-type");
            if (contentType && contentType.indexOf("application/json") !== -1) {
                return await res.json();
            } else {
                const text = await res.text();
                console.error("Non-JSON response from server:", text);
                return { success: false, message: "Lỗi hệ thống: Máy chủ phản hồi sai định dạng. Vui lòng xem Console (F12) để biết chi tiết." };
            }
        } catch (error) {
            console.error("Fetch error:", error);
            return { success: false, message: "Không thể kết nối đến máy chủ." };
        }
    }

    function approveOrder(id) {
        if (confirm('Bạn xác nhận đồng ý nhận phần đơn hàng có sẵn này?')) {
            handlePost('@Url.Action("CustomerApproveOrder", "Shop")?id=' + id)
            .then(data => {
                if (data.success) location.reload();
                else alert(data.message || 'Có lỗi xảy ra.');
            });
        }
    }

    function rejectOrder(id) {
        if (confirm('Bạn không đồng ý nhận phần đơn hàng này? Hệ thống sẽ hoàn tác và đưa đơn hàng gốc về trạng thái Chờ xử lý.')) {
            handlePost('@Url.Action("CustomerRejectOrder", "Shop")?id=' + id)
            .then(data => {
                if (data.success) location.reload();
                else alert(data.message || 'Có lỗi xảy ra.');
            });
        }
    }
</script>
}